# BD.Standard.CY.OpretionServicePlugIn

## 项目简介
金蝶BOS跨组织单据自动下推与审核服务插件。

## 功能说明
本插件主要用于处理跨组织单据的自动流转。当 `01.07.001` 组织的采购入库单审核后，自动触发 `01.07.002` 组织关联单据的生成与审核操作：

1. **销售出库单自动处理**：
   - 获取关联的 `01.07.002` 组织销售订单。
   - 下推生成销售出库单，更新实收数量与仓库（CK018）。
   - 自动提交并审核销售出库单。

2. **采购入库单自动处理**：
   - 获取关联的 `01.07.002` 组织采购订单。
   - 下推生成收料通知单，更新收料数量与仓库（CK018）。
   - 自动提交并审核收料通知单。
   - 将收料通知单下推生成采购入库单，并自动提交与审核。

3. **日志追踪**：
   - 自动记录业务操作节点与API交互结果。
   - 捕获并记录异常信息及堆栈，保障问题可追溯。

## 核心文件
- `AuditOperationPlugIn.cs`：核心业务逻辑，继承自 `AbstractOperationServicePlugIn`，实现单据下推与审核。
- `Logger.cs`：日志记录工具类，负责将运行信息写入本地文件。
